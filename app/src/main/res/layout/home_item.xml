<?xml version="1.0" encoding="utf-8"?>

<android.support.v7.widget.CardView
    android:id="@+id/card_view"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="200dp"
    android:layout_height="240dp"
    android:layout_gravity="center"
    card_view:cardCornerRadius="@dimen/item_corner_radius"
    card_view:cardUseCompatPadding="true"
    card_view:contentPadding="@dimen/compat_item_corner_radius">

    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@+id/iv_pic"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:placeholderImage="@color/material_blue_grey_800"
        app:pressedStateOverlayImage="@drawable/home_item_overlay"
        app:roundedCornerRadius="@dimen/item_corner_radius"/>

    <TextView
        android:id="@+id/tv_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@drawable/text_transparent_bg"
        android:lines="2"
        android:padding="5dp"
        android:textColor="#fff"/>

</android.support.v7.widget.CardView>